[graph]
all-features = true

[licenses]
allow = ["MIT", "Apache-2.0", "Unicode-3.0", "Zlib"]

[bans]
multiple-versions = "warn"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
# Until spacewalk is on crates.io; drop with the git dependency at release.
allow-git = ["https://github.com/dp88/spacewalk"]
